version: '2.2'
services:
  springboot-um:
    build:
      context: users/
      dockerfile: Dockerfile
    image: springboot-um-web
    ports:
      - "8080:8080"
    networks:
      - um-network
 
networks:
  um-network:
